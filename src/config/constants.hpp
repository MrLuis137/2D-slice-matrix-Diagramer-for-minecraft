#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <SFML/Graphics.hpp>

namespace Config{
    inline const sf::Color backgroundColor = sf::Color(13, 17, 23);
    inline const sf::Color fontColor = sf::Color(230,237,243);
    inline const sf::Color menuBGColor = sf::Color(22,27,34);
    inline const sf::Color buttonColor = sf::Color(88,166,255);
    inline constexpr float menuHigh = 40;
    inline constexpr float buttonHigh = 30;   
};

#endif